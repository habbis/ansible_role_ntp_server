---
- name: Converge
  hosts: all
  become: true
  gather_facts: yes
  #vars_files:
  #  - ../../defaults/main.yml
  #vars:


  tasks:
    - name: "Include ntp_server"
      include_role:
        name: "ntp_server"

  #roles:
    #- role: habbis.ntp_server
